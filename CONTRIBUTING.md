# Contribution

1. Créer une branche depuis `main`.
2. Ne jamais ajouter de clé API, certificat ou transcription privée.
3. Exécuter `pnpm typecheck` et `pnpm build:extension`.
4. Décrire clairement les changements dans la demande de fusion.
5. Vérifier les fonctions Windows sur Windows 10 ou 11.
